{
  "builders": [
    {
     "type": "digitalocean",
     "api_token": "{{user `api_token`}}",
     "ssh_username": "root",
     "image": "ubuntu-18-04-x64",
     "region": "nyc1",
     "size": "512mb",
     "snapshot_name": "ubuntu-18-04.3-corretto_8"
    }
   ],
 "provisioners": [
    {
	"destination": "/tmp/dev.edn",
	"source": "http/digital.edn",
	"type": "file"
    },
    {
      "execute_command": "echo '{{ user `ssh_password` }}' | {{.Vars}} sudo -E -S bash '{{.Path}}'",
      "scripts": [
        "scripts/update.sh",
	  "scripts/java.sh",
	  "scripts/re-ops-cloud.sh"
      ],
     "type": "shell"
    }
  ]
}
