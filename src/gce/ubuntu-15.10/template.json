{
  "builders": [
    {
	"ssh_pty" : "true",
	"type": "googlecompute",
	"account_file": "{{user `service_file`}}",
	"project_id": "{{user `project_id`}}",
	"source_image": "ubuntu-1510-wily-v20151021",
	"disk_size": "100",
	"image_description": "Ubuntu with puppet 3.8.2",
	"image_name": "ubuntu-1510-puppet-382-{{timestamp}}",
	"tags":["ssh-enabled"],
	"ssh_username": "ronen",
	"zone": "europe-west1-d"
   }
 ],
 "provisioners": [
    {
      "execute_command": "echo '{{ user `ssh_password` }}' | {{.Vars}} sudo -E -S bash '{{.Path}}'",
      "scripts": [
        "script/puppet.sh",
        "script/sshd.sh"
      ],
      "type": "shell"
    }
  ]

}
